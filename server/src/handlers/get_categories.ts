import { type Category, type TransactionType } from '../schema';

export const getCategories = async (type?: TransactionType): Promise<Category[]> => {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is fetching all categories from the database,
  // optionally filtered by transaction type (income or expense).
  return Promise.resolve([]);
};